import Hero from '../components/hero/Hero'

const HeroSection = () => {
  return (
    <section id="hero" className="scroll-mt-20 bg-brand-bg">
      <Hero />
    </section>
  )
}

export default HeroSection
